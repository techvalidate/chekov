%html
  %head
    %title Tester
    = stylesheet_link_tag 'application'
    = javascript_include_tag 'application'
    = csrf_meta_tags
    
  %body
    #Content
      #Main
        #Login
          %p
            = @current_user.name
            = link_to 'Logout', logout_path
          %p= link_to pluralize(@current_user.checks.from_release(@release).failed.count, 'Failed Test'), @current_user
        
        #Release= link_to @release.name, root_path
  
        = yield
