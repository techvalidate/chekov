%html
  %head
    %title Chekov
    = stylesheet_link_tag 'application'
    = javascript_include_tag 'application'
    = csrf_meta_tags
    
  %body
    #Content
      #Main
        #Login
          %p
            = @current_user.name
            = link_to 'Logout', logout_path
          %p= link_to 'Assignments', @current_user
        
        #Release= link_to @release.name, root_path
  
        = yield
